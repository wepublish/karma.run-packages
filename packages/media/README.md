# @wepublish/krp-media-server

# Public

`GET /:id`<br/>
`GET /:id/:filename.format`<br/>
`GET /:id/t/:transformations/:filename.format`<br/>

# Authorized

`POST /`<br/>
`PUT /:id`<br/>
`DELETE /:id`<br/>

`GET /_healthz`<br/>
